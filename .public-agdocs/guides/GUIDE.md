- use uv run to execute in this environment including launching the server and running pytest
    - to launch the server run `uv run app/main.py`
    - to run tests run `uv run pytest`
    - install all packages with `uv sync --all-extras`
    